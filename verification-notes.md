# ملاحظات التحقق

- بتاريخ 18 أغسطس 2026، استجابت مسارا `/receipts` و`/targets` في بيئة التطوير دون أخطاء بناء، لكن بيئة الالتقاط لا تحتوي جلسة مستخدم، ولذلك أعادت الصفحة إلى شاشة تسجيل الدخول بدلاً من الواجهات المحمية.
- أظهر فحص الأنواع نجاحاً بعد إضافة لوحة التصفية المتقدمة، التحديد، التصدير، سجل التدقيق، وصلاحية إدارة الأهداف.
- يلزم إجراء تحقق بصري لاحق من داخل جلسة مدير النظام أو مستخدم يملك صلاحية اعتماد السندات، دون تخطي المصادقة أو استخدام بيانات اعتماد غير مصرّح بها.
