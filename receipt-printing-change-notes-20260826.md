# ملاحظات تنفيذ تعديل سند القبض والمطبوعات

## النتائج المؤكدة

- جدول `receipts` يتضمن حقول العميل والمبلغ وطريقة الدفع والبنك و«وذلك مقابل» والملاحظات والحقول المخصصة، لكنه لا يحتوي حقلاً مستقلاً لبيان اختياري يظهر تحت اسم العميل؛ لذلك يلزم حقل نصي اختياري مستقل وترحيلة `ALTER TABLE` غير مدمرة، مع إبقاء قيمة الحقل فارغة للسندات الحالية.
- عقود `receipts.create` و`receipts.update` تستخدم إدخالاً منظمًا للإنشاء وتفويض المستخدم الحالي، فيما تستمر قواعد الإيصال وحالة الاعتماد كما هي. إضافة الحقل النصي لا تستلزم أي تغيير في قواعد الاعتماد أو البيانات القائمة.
- صفحة السند تستخدم حالياً حقلي `notes` و`statement` بمعنيين مختلفين؛ لذلك لا ينبغي إعادة استخدام `notes` للبيان الجديد حتى لا يتغير معنى الملاحظات أو شكل السندات السابقة.
- إعدادات تصميم المطبوعات موجودة بالفعل، وتتضمن ارتفاع الرأس والتذييل والنص والشعار واللون. ستُوسّع بإعداد اتجاه افتراضي رأسي ونسبتي مساحة الرأس والتذييل، مع تحويل قيم الواجهة الحالية إلى المتغيرات الجديدة بشكل متوافق.
- مولّد PDF الحالي يمرر الصفحة أفقياً في مسار السند، لذلك سيُعاد ترتيب محتوى السند ضمن مساحة محتوى طولية بين رأس وتذييل ثابتين بقيمة افتراضية 15% لكل منهما؛ وستبقى الإعدادات قابلة للتعديل من إعدادات النظام.
