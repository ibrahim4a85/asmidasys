# برومبت إعادة البناء المرجعي — منصة شركة الأسمدة المتحدة السعودية

> **طريقة الاستخدام:** انسخ هذا الملف كاملاً إلى نظام التطوير أو فريق البرمجة المكلف بإعادة بناء المنصة. يُعامل هذا الملف باعتباره مواصفة تنفيذ إلزامية، وليس مجرد وصف تسويقي. يجب تنفيذ ما هو موثق هنا، وطلب توضيح قبل افتراض أي قاعدة أعمال غير مذكورة.

## 1. المهمة والنتيجة المطلوبة

أعد بناء تطبيق ويب داخلي عربي باسم **«منصة شركة الأسمدة المتحدة السعودية»** لإدارة سندات القبض والصرف والعملاء والفروع والمستخدمين والصلاحيات والتعميدات والأهداف وتقارير الأداء والمتحصلات البنكية وحقوق الشركاء. يجب أن يكون التطبيق **RTL أولاً**، مناسباً للكمبيوتر والهاتف، ذا مظهر احترافي عملي مستلهم من أنظمة ERP السحابية الحديثة مثل دفترة، لكنه ليس WordPress ولا قالباً عاماً ولا منتج SaaS جاهزاً.

نفّذ التطبيق كمشروع مخصص قابل للنقل والاستضافة الذاتية. لا تستخدم بيانات تجريبية في الإنتاج، ولا تنشئ مراجعات أو تقييمات أو محتوى مستخدم مُختلق. يجب أن تدعم المنصة استعادة البيانات الواقعية فقط عبر ترحيل قاعدة بيانات ومرفقات منفصلين ومؤمّنين، ولا يجوز تضمين كلمات مرور أو مفاتيح API أو ملفات مرفقة أو سجلات مالية فعلية داخل هذا البرومبت أو الشيفرة المصدرية.

## 2. التقنية المعتمدة والقيود المعمارية

| الطبقة | المطلوب |
|---|---|
| الواجهة | React 19 + TypeScript + Vite، مع Tailwind CSS وshadcn/Radix للمكونات التفاعلية. |
| الخادم | Node.js 22+ مع Express وtRPC؛ تبقى عقود الإدخال والتحقق في الخادم ولا يكتفى بحجب عناصر الواجهة. |
| قاعدة البيانات | MySQL 8+ أو TiDB المتوافق مع MySQL، باستخدام Drizzle ORM وترحيلات SQL مرقمة غير مدمرة. |
| المصادقة | جلسة نظام مخصصة عبر cookie آمن باسم منطقي مماثل لـ`system_session`، تتضمن انتهاء صلاحية وإبطال تسجيل الخروج. لا تُبنَ الوظائف الجديدة على OAuth القالبي. |
| التخزين | Object Storage متوافق S3 للمرفقات والشعارات والخلفيات. تحفظ قاعدة البيانات المفاتيح والروابط والاسم وMIME فقط، ولا تحفظ بايتات الملفات داخل أعمدة SQL. |
| المستندات | jsPDF/Canvas أو بديل موثوق يدعم العربية وRTL. يُولد PDF في الخادم أو العميل وفق الحاجة، مع مطابقة صارمة للمعاينة. |
| الاختبارات | Vitest للوحدة والتكامل والمواصفات المصدرية، وفحص TypeScript وبناء إنتاج وفحص فروق قبل كل إصدار. |

استخدم طبقات واضحة: `client/` للواجهة، `server/` للإجراءات وقواعد العمل، `drizzle/` للمخطط والترحيلات، و`shared/` للثوابت والقواعد المشتركة. يجب أن تكون الأدوار والحدود والصلاحيات قابلة للاختبار ولا تكون شيفرة الواجهة هي مصدر الحقيقة.

## 3. الهوية البصرية وتجربة المستخدم

### 3.1 اتجاه التصميم

اللغة الافتراضية عربية واتجاه كل الواجهات **من اليمين إلى اليسار**. استخدم خطاً عربياً واضحاً، ومسافات مريحة، وتسميات مفهومة لطاقم الفروع والإدارة. اجعل لوحة الألوان مهنية وحيوية: أزرق/نيلي كأساس، تركواز أو أخضر للحالات الإيجابية، كهرماني للتنبيه، وأحمر للرفض والحذف. يجب تفعيل الوضع الداكن والوضع الفاتح مع حفظ الاختيار للمستخدم.

لا تستخدم صفحات مركزية عامة أو بطاقات مبالغاً فيها. استخدم تخطيط ERP: قائمة جانبية قابلة للتمرير وتغيير العرض، شريط علوي فيه الحساب والإشعارات وتبديل المظهر، ومساحة محتوى تستوعب الجداول والتقارير. تدعم القائمة الجانبية سحب عرضها وحفظ العرض لكل مستخدم ضمن نطاق تقريبي 220–560px. يجب أن تبقى قابلة للتمرير داخلياً في شاشات الهاتف والارتفاعات الصغيرة.

### 3.2 عناصر التخصيص البصري

يتحكم مدير النظام في المظهر من الإعدادات: النمط القديم للقائمة/لوحة التحكم والنمط الحديث، الوضع الداكن، لون عنصر المرور أو الاختيار من خمسة ألوان عصرية، إعدادات شاشة الدخول، الشعار، الخلفيات، مكان وحجم زر الوضع الداكن في شاشة الدخول، واسم الشركة المعروض. يجب تطبيق التغييرات فور الحفظ عبر tokens أو CSS variables، مع ضمان قابلية قراءة النص في الوضعين.

تدعم لوحة التحكم اختصارات قابلة للترتيب لكل مستخدم، وأقساماً/عناصر قابلة للسحب وإعادة الترتيب مع حفظ التفضيل الشخصي. صممها كبطاقات مؤشرات مختصرة، اختصارات إجراءات، ومناطق تقارير قابلة للتخصيص، دون تغيير ترتيب باقي المستخدمين ما لم يكن الإعداد عاماً.

### 3.3 التنقل والمسارات

أنشئ المسارات التالية، وكلها محمية بجلسة النظام وصلاحية القائمة أو الصلاحية الدقيقة الملائمة: `/dashboard`، `/users`، `/receipts`، `/payments`، `/customers`، `/targets`، `/settings`، `/delegates`، `/reports`، `/partner-rights`، `/performance-reports`، `/notifications`. المسار `/` صفحة تسجيل الدخول. لا يظهر رابط في القائمة لمن لا يملك الوصول إليه، ويعاد توجيه محاولات الوصول اليدوية إلى لوحة التحكم.

## 4. الأدوار والصلاحيات

### 4.1 أنواع المستخدمين

ابدأ بالأنواع القابلة لإعادة التسمية والتوسيع: **مدير النظام، مراجع، مراجع معمد، مندوب، مدير مبيعات، مدير مالي، شريك مفوض**. يوفر مدير النظام جميع الصلاحيات دائماً. يمكنه إنشاء نوع مستخدم جديد، اختيار فئته، وتعديل مسماه، وتعيين صلاحياته. لا تُعامل أسماء الأنواع كنصوص ثابتة داخل قواعد الأعمال؛ استخدم معرفاً ثابتاً ومسمى قابل للتعديل.

احتفظ ببيانات المستخدم: اسم المستخدم الفريد، كلمة مرور مجزأة، الاسم الكامل، نوع المستخدم، المنصب/صفة مجلس الإدارة إن وجدت، المستوى رئيسي/فرعي، الفرع، الهاتف والبريد، النشاط، منشئ/آخر معدل، آخر دخول، ومصفوفة صلاحيات JSON. يجب أن يكون تعديل المستخدم نافذة RTL سليمة وقابلة للتمرير، مع زر فعلي لتحديث القائمة بعد التعديل.

### 4.2 الصلاحيات الدقيقة

بالإضافة لصلاحية عناصر القائمة، نفّذ صلاحيات دقيقة منفصلة لمجالات: إدارة العملاء، المستهدفات، إصدار/تعديل/حذف سندات القبض، اعتماد/رفض السندات، طباعة/تنزيل المستندات، إدارة التعميدات، طلب التعميد، عرض التقارير، إدارة إعدادات النظام، إدارة المستخدمين، إدارة النسخ الاحتياطية، وإدارة حقوق الشركاء.

في حقوق الشركاء، افصل على الأقل بين: `partnerRightsViewReport` و`partnerRightsCreateEntries` و`partnerRightsEditEntries` و`partnerRightsManagePartners` و`partnerRightsManageAllocations` و`partnerRightsManageAttachments`. الشريك المفوض يرى تقريره فقط افتراضياً؛ لا يستطيع رؤية بيانات شركاء آخرين أو تعديلها. يمكن للمدير منح صلاحيات إضافة أو تعديل لمراجع أو مراجع معمد أو نوع آخر دون منح تلقائي.

## 5. البيانات الأساسية ومخطط قاعدة البيانات

نفّذ الجداول التالية مع مفاتيح أساسية رقمية، حقول تتبع `createdAt/updatedAt/createdBy/updatedBy` حيث يلزم، وفهارس فريدة موثقة. استخدم decimal للمبالغ ولا تستخدم float.

| المجال | الكيانات والحقول الجوهرية |
|---|---|
| الهوية | `system_users`: username فريد، password hash، fullName، userType، boardPosition، userLevel، branchId، isActive، permissions JSON، phone، email، audit timestamps. جدول `users` القالبي إن لزم للتوافق فقط. |
| الإعدادات | `system_settings(settingKey unique, settingValue, updatedAt)`، و`company_profile(companyName,address,commercialRegister,taxNumber,logoUrl,phone)`. |
| التنظيم | `regions`، `branches(name,address,regionId,receiptSerial,paymentSerial,currentReceiptNumber,currentPaymentNumber,isActive)`، و`financial_periods(name,startDate,endDate,isActive)`. |
| العملاء | `customers(customerNumber unique,name,branchId,delegateId,phone,email,address,isActive, audit)`؛ الترتيب الافتراضي في العرض حسب رقم العميل تصاعدياً مع بحث بالرقم أو الاسم. |
| القوائم | `input_lists(listType: payment_method|bank|payment_for,name,parentId,requiresAttachment,isActive,sortOrder)`. تدعم شجرة القائمة الرئيسية والقوائم الفرعية. |
| المقبوضات | `receipts(serialNumber,branchId,userId,customerId,amount,amountInWords,paymentMethodId,bankId,paymentForId,customerStatement nullable,attachmentUrl,qrCode,status draft|issued|approved|rejected,notes,customFields,periodId,audit)` و`receipt_invoices(receiptId,invoiceNumber,invoiceAmount)` و`receipt_decision_audit(receiptId,decision,previousStatus,nextStatus,decidedBy,note,createdAt)`. |
| المصروفات | `payments(serialNumber,branchId,userId,beneficiary,amount,amountInWords,expenseCategory,paymentMethodId,bankId,description,attachmentUrl,status draft|approved|rejected,notes,periodId,audit)`. |
| الأداء | `performance_reports(branchId,reportDate,salesInvoicesCount,salesInvoicesValue,returnsInvoicesCount,returnsInvoicesValue,netSalesValue,receiptsCount,collectionsValue,audit)` مع فهرس فريد `(branchId,reportDate)`. |
| الإشعارات والسجل | `internal_notifications(recipientUserId,notificationType,title,message,receiptId,approvalRequestId,isRead,readAt,createdAt)`، وسجل نشاط عام و`backup_logs`. |
| التعميدات | `document_types`، `delegates`، `approval_workflows`، `approval_workflow_reviewers`، `approval_workflow_approvers`، `approval_requests`، `approval_request_reviewer_steps`، `approval_request_steps`، حسب القواعد في القسم 9. |
| الشركاء | `partners`، `partner_period_settings`، `partner_period_allocations`، `partner_account_entries` حسب القسم 10؛ الملفات روابط S3 فقط. |
| واجهة الدخول | `news_items` و`login_backgrounds` عند الحاجة للرسائل والخلفيات التي يديرها النظام. |

لا تُخرج محتوى قاعدة البيانات الحية إلى ملف المواصفات. عند النقل أو الاستعادة، استخدم تصديراً منفصلاً مشفراً لقاعدة البيانات، ومظهراً للملفات الكائنية، وجدول تحقق للأعداد والتجزئات. يجب أن تنجح المنصة دون بيانات أولية حساسة، مع بيانات إعداد مرجعية غير إنتاجية أو عبر معالج إعداد أولي.

## 6. إدارة العملاء والفروع والقوائم والمستهدفات

### 6.1 العملاء

توفر الشاشة إنشاء العميل وتعديله وتعطيله وحذفه بعد تأكيد صريح وصلاحية منفصلة. تعرض جدولاً RTL قابلاً للبحث والفرز، مع تمييز عمود الرقم وإظهار الفرع والمندوب. ينتمي عملاء المندوب إلى المندوب المرتبط به؛ لا يرى المستخدم الفرعي العملاء خارج نطاقه إلا بصلاحية صريحة. أضف استيراد Excel من قالب قابل للتحميل: معاينة صفوف قبل الاعتماد، رسائل أخطاء لكل صف، تحديث العملاء المطابقين وفق الرقم أو إنشاء الجدد وفق الخيار المصرح، وتصدير النتائج إلى Excel/PDF.

### 6.2 الفروع والفترات والسيريلات

رتب الفروع في كل القوائم والتقارير بالترتيب: **الرياض، بريدة، الخرج، جدة، وادي الدواسر، تبوك، حائل، الدمام، نجران، الجوف**. كل فرع له عداد سند قبض وعداد صرف. رقم سند القبض سنوي وبصيغة تعتمد الفرع والسنة والعداد مثل `3-26-001`. سند الصرف يستعمل بادئة `PY` مع كود الفرع والترقيم المتسلسل وفق صيغة متفق عليها. يجب منع التنافس عند زيادة العداد باستخدام معاملة قاعدة بيانات/قفل مناسب.

### 6.3 قوائم الإدخال الشجرية

في الإعدادات، أدِر ثلاث قوائم رئيسية على الأقل: طرق الدفع، البنوك، وذلك مقابل. تدعم كل قائمة عنصراً رئيسياً وقوائم فرعية، الترتيب، التعطيل، وإعادة التسمية. من طرق الدفع المرجعية: تحويل بنكي، نقداً، إيداع شبكة، إيداع نقدي، إيداع شيك. **النقد فقط** لا يطلب مرفق إيصال التحويل؛ بقية الطرق ذات `requiresAttachment=true` تطلب مرفقاً قبل الإصدار. لا تحذف عقدة لها استخدامات تاريخية؛ عطلها أو استخدم سياسة ترحيل محكومة.

### 6.4 الأهداف

أدر أهداف كل فرع وفترة: مبيعات أو متحصلات أو هدف مخصص، بالقيمة المستهدفة والحالية، مع اختيار الفترة والفرع والصلاحيات. يجب تمكين الحذف الانتقائي بعد تأكيد ونسخة احتياطية اختيارية إن كان نطاق الحذف واسعاً.

## 7. سندات القبض: سير العمل والشاشة

### 7.1 الإصدار والبيانات

توفر صفحة سندات القبض تبويبات أو اختصارات مضغوطة: **إصدار سند قبض**، **عرض السندات**، ومنطقة تصفية قابلة للطي افتراضياً وتفتح اختيارياً أو عند تحديد سجلات. يجب أن يمثل سجل السندات غالبية مساحة الشاشة. يحتوي نموذج السند على الفرع، العميل، المبلغ، المبلغ كتابةً عربياً، طريقة الدفع، البنك، وذلك مقابل، بيان اختياري للعميل، الفواتير المسددة (إن وجدت)، ملاحظات، حقول مخصصة، والمرفق المطلوب وفق طريقة الدفع.

استبدل تسمية «الفواتير المسددة» الظاهرة للمستخدم بـ«البيان» مع الإبقاء على تسجيل أرقام الفواتير ومبالغها. إذا لم توجد فواتير، يظل البيان النصي المسجل في السند ظاهراً، مثل «سداد الرصيد» أو «دفعة من الحساب». يسمح البيان الاختياري `customerStatement` بطول محدود (300 حرف تقريباً)، ويحفظ دون تعديل السندات القديمة التي تبقى قيمته فيها NULL.

### 7.2 سجل السندات والأدوات

اعرض جدولاً قابلًا للبحث والتحديد المتعدد والفرز من رؤوس الأعمدة، خصوصاً التاريخ والبنك وطريقة الدفع. يجب السماح بسحب حدود الأعمدة لتصغيرها وتوسيعها وحفظ المقاس لكل مستخدم. يكون عمود المبلغ أعرض وخطه أكبر وأثقل. لا تضع عمود إيصال مستقل؛ اجمع الأدوات في عمود إجراءات أفقي ملوّن ومختصر قدر الإمكان.

توفر إجراءات كل صف: معاينة الإيصال، تحميل السند، تحميل السند مع الإيصال، اعتماد السند، رفض السند، تعديل السند، وسجل الطباعة/القرار. تظهر أزرار اعتماد/رفض مصغرة أيضاً في خلية الحالة حيث تسمح الصلاحية. الرفض يطلب سبباً اختيارياً أو إلزامياً وفق السياسة ويحفظه في السجل، ويصدر إشعاراً. يدعم التحديد المتعدد والاعتماد الدفعي مع حراسة خادمية لكل سجل. لا تغير حالة سند لا يملك المستخدم صلاحية قراره.

### 7.3 التصفية والتقارير المرتبطة

يدعم السجل التصفية بالفترة والفرع والحالة والبنك وطريقة الدفع والمستخدم والعميل والتاريخ، إضافة للبحث النصي. لا تظهر للمندوب إلا سجلات فرعه أو نطاقه حسب الصلاحيات. سجل قرارات وتدقيق لكل اعتماد أو رفض مع المستخدم والتاريخ والحالة السابقة واللاحقة والسبب.

## 8. المطبوعات والمعاينة والتنزيل

### 8.1 إعدادات مشتركة

أنشئ إعدادات مطبوعات مركزية قابلة للإدارة: عنوان الشركة، الشعار ومكانه وحجمه، موضع/حجم QR، مكان العنوان، تذييل موحد، اتجاه الصفحة، ونسب مساحة الرأس والتذييل. يكون الافتراضي لكل المطبوعات **عمودياً Portrait** مع **15% للرأس و15% للتذييل**، مع السماح للمدير بالتعديل بنطاق آمن (مثل 10–25%). تطبق الإعدادات على سندات القبض وتقارير PDF.

تخزن التسميات القابلة للتعديل في قاموس مركزي. يجب أن تظهر مفاتيح التسميات المتاحة في صفحة إدارة التسميات، ومنها `receipt_print_received_from` الافتراضي **«استلمنا من المكرم»**. يجب أن ينعكس التجاوز فوراً في المعاينة وPDF دون تعديل الشيفرة.

### 8.2 قالب سند القبض

المعاينة HTML وPDF يجب أن يعتمدا السجل التفصيلي نفسه وإعدادات الشركة نفسها، لا صف القائمة المختصر. يدعم القالب الشعار وQR واسم الشركة في سطر واحد قدر الإمكان وبيانات السند والعنوان وجدولاً منظمًا وتذييل المستلم. لا تقسّم اسم الشركة بسبب ضيق الحاوية؛ استخدم تصغيراً تدريجياً أو صفاً مستقلاً مناسباً. تكون نافذة المعاينة عمودية معتدلة بتمرير رأسي داخلي واضح، ومتجاوبة للهاتف.

**قاعدة إلزامية أخيرة:** تعرض عبارة **«استلمنا من المكرم»** مستقلة فوق جدول التفاصيل وبمفتاح قابل لإعادة التسمية. داخل صف **«اسم العميل»** وحده، تعرض قيمة الخلية اسم العميل ثم `customerStatement` الاختياري في السطر التالي (`white-space: pre-line`). لا تنشئ كتلة مستقلة للاسم أو البيان أسفل عبارة الاستلام. يحافظ مولد Canvas/PDF على فواصل الأسطر، ويزيد ارتفاع صف العميل تلقائياً عندما تتضمن قيمته سطرين.

ينزّل إجراء «تحميل السند مع الإيصال» ملفين PDF متتاليين بالاسمين `${serial}.pdf` و`${serial}-ESAL.pdf`، ولا يكتفي بفتح رابط المرفق الخام. في التحديد المتعدد، ينشئ ZIP يحتوي لكل سند الملفين بالأسماء نفسها. يجب التحقق من ظهور الشعار وQR داخل ملف PDF المحفوظ، لا في المعاينة فقط.

## 9. نظام التعميدات

استبدل مفهوم «إدارة المفوضين» المحدود بسير تعميد قابل للتهيئة. نوع التعميد قابل للإنشاء والتعديل والحذف المنضبط، ويحدد: اسم النوع، إلزام النموذج أو اختياريته، نموذج مرجعي Word/Excel/PDF قابل للرفع، عدد المراجعين وقائمتهم وترتيبهم (متسلسل أو متوازٍ)، الحد الأدنى لعدد المفوضين وقائمتهم وترتيبهم، وصلاحية كل مشارك حسب نوع التعميد.

ينشئ المستخدم صاحب صلاحية الطلب طلباً برقم متسلسل مثل `RA101-001` مع مرفق. تحفظ كل دورة التعميد المرجع والمرفق، ويكون المرفق مرئياً للمراجع والمفوض داخل نافذة الطلب. وفر معاينة PDF مباشرة داخل النافذة بدلاً من إجبار فتح تبويب جديد. عند رفع ملف Word/Excel/PDF، أضف رقم التعميد آلياً على المستند عند المعاينة والطباعة عبر طبقة أو ختم متوافق بحسب النوع، ولا تستبدل الأصل دون الاحتفاظ بمرجع النسخة.

تظهر حالة مسار واضحة: تم الإرسال، قيد المراجعة، تمّت المراجعة والإرسال للمفوضين، تم التعميد من الاسم/المنصب، لم تكتمل الاعتمادات اللازمة، مكتمل، مرفوض. يضيف المراجع والمفوض ملاحظة اختيارية للموافقة أو الرفض وتظهر في السجل. عند اكتمال التعميدات، يضاف في تذييل النسخة المعتمدة تقسيم إلى أربعة أجزاء قابلة للضبط، كل جزء يتضمن «تم التعميد»، اسم المفوض، التاريخ والساعة؛ ويجب أن تكون هذه الطبقة اختيارية ضمن إعداد الدورة.

توفر شاشتان لمن لديه الصلاحية فقط: **طلب تعميد** و**حالة الطلبات**. يكون لمدير النظام حق حذف سجل التعميدات التجريبي ضمن إجراء حذف محكوم، مع صلاحية مستقبلية مستقلة. لا تفقد الملفات أو خطوات التدقيق عند تغيير حالة الطلب.

## 10. حقوق الشركاء

أضف رابط «حقوق الشركاء» للقائمة الجانبية، يظهر للشريك المفوض حتى لو كانت إعدادات القائمة القديمة لا تحتوي المعرف الجديد، مع احترام الحجب الصريح. تحتوي الصفحة ثلاث تبويبات RTL مرتبة بحيث يظهر **تقرير توزيعات الأرباح يميناً** و**إدارة حصص الشركاء يساراً**:

| التبويب | السلوك المطلوب |
|---|---|
| تقرير توزيعات الأرباح | يختار الفترة ويعرض الرصيد الافتتاحي للأرباح المرحلة، نسب الحصص، حركات الحساب الجاري، الرصيد الختامي، وسجل توزيع/مسحوبات واضح. الشريك يرى بياناته فقط. |
| إضافة سجل | يسجل مسحوبات أو استحقاقات أو تسوية نهاية العام لكل شريك: التاريخ، البيان، المبلغ، الاتجاه المحاسبي عند التسوية، ومرفق اختياري. لا تخزن إشارة سالبة؛ النوع والاتجاه يحددان الأثر. |
| إدارة حصص الشركاء | يدير الشركاء وصورهم الاختيارية، ربط الحساب بالشريك، الحصص لكل فترة، الرصيد المرحل، وقرار توزيع الأرباح ومرفق تسوية نهاية العام. يمنع الفهرس حصتين للشريك نفسه في الفترة. |

تتضمن الشركاء المعتمدين عند إدخال البيانات: طارق عبدالرحمن مراد، أحمد طارق عبدالرحمن مراد، مروان طارق عبدالرحمن مراد، ميساء طارق عبدالرحمن مراد. يُعامل «لارين» كشريك فقط غير مفوض، للاطلاع على تقاريره وفق الصلاحية وليس مفوضاً في التعميدات تلقائياً. لا تزرع هذه الأسماء في بيئة جديدة إلا بقرار مالك النظام؛ هي بيانات تهيئة عمل وليست متطلباً تقنياً دائماً.

## 11. سندات الصرف، التقارير، الأداء والمتحصلات

توفر شاشة سندات الصرف بإصدار وبيانات المستفيد والمبلغ كتابةً وفئة المصروف وطريقة الدفع والبنك والوصف والمرفق والحالة، مع ترقيم الفرع ونمط بادئة `PY`. توفر تقارير PDF/Excel منظمة وفق إعدادات الطباعة المشتركة.

توفر صفحة تقارير الأداء إدخالاً يومياً لكل فرع: التاريخ، عدد وقيمة المبيعات، عدد وقيمة المرتجعات، صافي المبيعات المحسوب، عدد سندات القبض وقيمة المتحصلات. تمنع السجل المكرر للفرع/اليوم، وتسمح لمن لديه صلاحية بتعديل تاريخ سابق. يرى المدير تبويبات كل الفروع، ويستطيع المندوب/مدير الفرع إدخال نطاقه فقط.

توفر تقارير متحصلات بنكية يومية مع فلاتر البنك والفترة: اليوم (الافتراضي)، أمس، آخر أسبوع، وفترة مخصصة. تظهر النتائج وإجمالياتها مباشرة عند تغيير الفلتر. وفر تقرير أداء بنفس خيارات الفترة والفرع وإجماليات المبيعات والمرتجعات والمتحصلات، وفلتر «الفروع الأعلى تحصيلاً» يختار اليوم افتراضياً ثم أمس وأسبوع سابق وفترة مخصصة.

## 12. النظام الإداري والإشعارات والنسخ الاحتياطي

توفر صفحة إعدادات بعناوين RTL ومناطق واضحة للتحكم في: ملف الشركة، المظهر، التسميات، القوائم الشجرية، الحقول المخصصة لسند القبض، تخطيط السند والمطبوعات، استيراد العملاء، أنواع المستخدمين والصلاحيات، إعدادات دورة التعميد، النسخ الاحتياطي، وخلفية/شاشة الدخول. يجب تمييز لون التسمية الحالية ولون حقل تعديلها بوضوح لتفادي اللبس.

قدم وظائف حذف انتقائي للعملاء أو السندات أو الأهداف أو بيانات التعميدات وفق صلاحيات منفصلة وتأكيد مكتوب. قبل عمليات الحذف الواسع، قدم خياراً اختيارياً لإنشاء سجل أو نسخة احتياطية أولاً، ولا تحذف المستخدمين أو العملاء عند تنظيف البيانات التجريبية إلا إذا اختار المدير ذلك صراحة.

يظهر جرس الإشعارات بجوار الحساب في الشريط العلوي. بالضغط على إشعار ينتقل المستخدم إلى مصدره (سند أو طلب تعميد) بعد التحقق من الصلاحية. تحفظ الإشعارات حالة القراءة وتاريخها، ولا تكشف كياناً محمياً لمستخدم غير مخول.

## 13. البيانات والمرفقات والاستعادة الآمنة

لا تحتوي مواصفة إعادة البناء على «كل البيانات» بمعنى قيم الإنتاج، لأن ذلك يعرض العملاء والحركات والمرفقات للخطر ويصبح قديماً فوراً. بدلاً من ذلك، أنشئ **حزمة استعادة منفصلة** ملكاً للشركة تحتوي على:

1. تصدير MySQL متسق ومشفّر مع تاريخ وتجزئة SHA-256 وعدد صفوف لكل جدول.
2. جرد Object Storage بالمفتاح والحجم وMIME والتجزئة، ونسخة الملفات في مساحة تخزين مملوكة للشركة.
3. ملف mapping يربط مفاتيح S3 بالصفوف، من دون روابط عامة دائمة.
4. ملف إعدادات غير سرية للعرض والقوائم والتسميات، وأسرار بيئة تسلّم عبر مدير أسرار فقط.
5. نص إجراء استعادة على بيئة اختبار أولاً، يتضمن تحقق العدّ والتجزئة وفحص الدخول والتقارير والمرفقات.

## 14. واجهات API وقواعد الخادم

استخدم tRPC أو REST مضبوط الأنواع، وتحقق Zod/Validator من كل إدخال. نظّم المجالات كإجراءات مستقلة: auth/session، users، userTypes، companyProfile/settings/labels، regions/branches/financialPeriods، customers/import/export، inputLists، receipts/invoices/attachments/decisions/printLog، payments، performanceReports، bankCollections/reports، delegates/documentTypes/approvalWorkflows/approvalRequests، partners/partnerPeriods/partnerEntries، notifications، backups.

يحمل الخادم دائماً هوية المستخدم من الجلسة، ويطبق التحقق من النوع والفرع والملكية والصلاحيات الدقيقة قبل القراءة والكتابة والتنزيل. استخدم pagination واستعلامات مفهرسة في السجلات الكبيرة. امنع القيم غير الصحيحة: مبلغ موجب، فواتير تابعة للسند فقط، نسب شركاء قابلة للتحقق، بيان العميل بحد طول، ملفات بحجم وMIME مسموح، ومسار مرفق لا يُكشف دون authorization.

## 15. الاختبارات ومعايير القبول

أنشئ اختبارات Vitest لكل قاعدة حساسة، ومنها: جلسة النظام وتسجيل الخروج، الأنواع والصلاحيات والقائمة الجانبية، وصول الشريك المفوض، ترتيب الفروع، فرز العملاء، استيراد/تصدير العملاء، طرق الدفع وشرط المرفق، توليد السيريلات، اعتماد/رفض السند وسجل القرار، تنزيل السند/الإيصال وZIP، تكامل قالب PDF، إعدادات الطباعة، التنبيهات، التقارير اليومية، التعميدات متعددة المراجعين/المفوضين، النسخ الاحتياطي، وحسابات الشركاء.

اختبار إلزامي خاص بسند القبض: تحقق أن `receipt_print_received_from` يستعمل القيمة الافتراضية أو التجاوز الإداري، وأن عبارة الاستلام مستقلة، وأن `customerIdentity = customer.name + '\n' + customerStatement` يوضع في قيمة صف «اسم العميل» في المعاينة وPDF، وأن دالة التفاف النص تحافظ على السطر الجديد، وأن السندات القديمة بلا بيان تظل صحيحة.

قبل كل إصدار شغّل بالتتابع: الاختبارات المستهدفة، `pnpm check`، `pnpm test`، `pnpm build`، و`git diff --check`. لا تنفذ اختبارات طباعة/تنزيل/اعتماد على سجلات إنتاج قائمة دون تفويض صريح؛ استخدم نسخة اختبار أو تحققاً قرائياً.

## 16. النشر والصيانة وقابلية النقل

اجعل المشروع قابلاً للنشر على VPS أو سحابة مُدارة: Node.js 22+، 4 vCPU، 8 GB RAM، 80 GB NVMe كبداية، MySQL 8+، Object Storage S3 يبدأ من 100 GB، نطاق/SSL، DNS وحسابات وفوترة باسم الشركة، مراقبة uptime وسجلات مركزية ونسخ يومية واختبار استعادة ربع سنوي. افصل production عن staging، ولا تعمل على الإنتاج مباشرة.

استخدم Git خاصاً بالشركة، فروعاً للإصلاح والميزات، مراجعة شيفرة، CI لتشغيل الاختبارات، متغيرات بيئة في مدير أسرار، وإصداراً مرقماً مع rollback. دورة التغيير: طلب واضح → تحليل أثر → تطوير في فرع → اختبار آلي ويدوي → نسخة احتياطية/ترحيل مدروس → قبول مالك النظام → نشر → مراقبة → توثيق. لا تستخدم `git reset --hard` لمعالجة الإنتاج؛ اعتمد rollback لنقطة إصدار معروفة.

## 17. مخرجات إعادة البناء المطلوبة

سلّم: مستودع Git كامل؛ دليل تشغيل ونقل؛ مخطط ERD؛ ترحيلات SQL؛ ملف `.env.example` بلا أسرار؛ بنية Object Storage؛ اختبارات؛ دليل صلاحيات؛ دليل إدارة للمستخدم؛ قوالب Excel؛ صفحات RTL المتجاوبة؛ وسجل إصدارات. بعد كل مرحلة، قدم قائمة قبول قابلة للاختبار. لا تعلن اكتمال ميزة لم تتحقق منها أو لم تؤكدها الإدارة.

## 18. حدود هذه المواصفة ومصدر الحقيقة

هذه المواصفة تصف حالة المنصة الوظيفية حتى آخر تصحيح موثق: نقل بيان العميل الاختياري إلى خلية «اسم العميل» في سند القبض، مع إبقاء عبارة الاستلام قابلة للتعديل. لا تستبدل النسخ الاحتياطية أو المصدر البرمجي الفعلي؛ عند وجود تعارض، يكون **المخطط والترحيلات والاختبارات في المستودع المعتمد** هما مصدر الحقيقة التقني، وتكون **قرارات الشركة المكتوبة** مصدر الحقيقة التجاري. اطلب قراراً صريحاً قبل حذف بيانات أو تنفيذ قرار مالي أو استعمال بيانات إنتاج في بيئة اختبار.
