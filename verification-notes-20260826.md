# ملاحظات تحقق النطاق الرسمي — 26 أغسطس 2026

- بعد حفظ الإصدار `4350c7e7`، فُتحت صفحة `https://asmidasys.com/receipts?release=4350c7e7` بحساب مدير النظام في وضع قراءة فقط.
- ظلت الصفحة المنشورة تعرض عمود «الإيصال» المستقل ومقبض تحجيم «المبلغ» فقط، ما يدل على أن المتصفح أو CDN ما زال يحمّل حزمة الأصول السابقة رغم وصول النسخة الخادمية.
- لم تُضغط أزرار اعتماد أو رفض أو تعديل أو تنزيل، ولم تتغير أي بيانات مالية أو قرارات.
- الإجراء التالي: رفع معرّف أصول الإنتاج ثم إعادة بناء وحفظ إصدار جديد، وبعدها يعاد التحقق المرئي للجدول وللقائمة الجانبية بحسابات مخولة.

## إعادة التحقق بعد إبطال التخزين المؤقت — الإصدار 11a6662b

- بعد نشر الإصدار `11a6662b`، فُتحت صفحة `https://asmidasys.com/receipts?release=11a6662b` بحساب مدير النظام في وضع قراءة فقط.
- وصلت الحزمة الجديدة إلى `asmidasys.com`: اختفى عمود «الإيصال» المستقل، وظهرت مقابض تحجيم محفوظة لجميع الأعمدة (التحديد، الرقم، التاريخ، العميل، المبلغ، الدفع، البنك، البيان، الحالة، والإجراءات)، كما ظهرت اختصارات معاينة الإيصال وتنزيل السند وتنزيل السند مع الإيصال ضمن عمود الإجراءات.
- ظهر مقبض تغيير عرض القائمة الجانبية، وبقيت عناصر التنقل قابلة للوصول في القائمة؛ لا تدعم أدوات التحقق الحالية تنفيذ سحب مؤشر آمن لتسجيل قياس عملي، بينما تؤكده اختبارات الانحدار والواجهة المنشورة.
- فُتحت صفحة `https://asmidasys.com/partner-rights?release=11a6662b` بحساب مدير النظام دون خطأ وصول، وظهرت تبويبات تقرير توزيعات الأرباح وإضافة سجل وإدارة حصص الشركاء، مع بيانات التقرير القائمة فقط.
- لم تُضغط أزرار اعتماد أو رفض أو تعديل أو إنشاء أو تنزيل، ولم تتغير أي بيانات مالية أو قرارات أو مستخدمين أو صلاحيات.
- ما زال مطلوباً تحقق حي بحساب شريك مفوض وبحساب موظف مُنحت له إحدى الصلاحيات التفصيلية؛ لا يمكن إجراؤه من جلسة مدير النظام الحالية دون تسجيل الدخول بالحسابين، وسيبقى في وضع القراءة فقط.

## التحقق المنشور للإصدار fabbc781

- في `https://asmidasys.com/receipts?release=fabbc781` ظهر سجل سندات القبض بحساب مدير النظام، مع مقابض تحجيم للأعمدة ومن دون عمود إيصال مستقل. ظهرت اختصارات معاينة الإيصال والتحميل والتنزيل المزدوج في عمود الإجراءات. لم يُنقَر أي زر اعتماد أو رفض أو تعديل أو تنزيل.
- في `https://asmidasys.com/partner-rights?release=fabbc781` ظهر تبويب «تقرير توزيعات الأرباح» في الطرف الأيمن و«إدارة حصص الشركاء» في الطرف الأيسر في واجهة RTL. لم تُنشأ أو تُعدل أي حصة أو حركة أو مرفق.
- يحتاج البيان الاختياري للعميل والقالب الرأسي لملف PDF إلى تجربة مستخدم عبر إنشاء سند جديد أو إعادة طباعة سند قائم؛ لم يُشغّل هذا الإجراء آلياً حفاظاً على السجلات وسجل الطباعة.

## التحقق التقني لتصحيح موضع بيان العميل

- أصبحت عبارة «استلمنا من المكرم» تسمية مستقلة قابلة للتعديل عبر المفتاح `receipt_print_received_from`، بينما يُجمع اسم العميل والبيان الاختياري داخل قيمة خلية «اسم العميل» في المعاينة وPDF.
- نجحت اختبارات الانحدار وفحص TypeScript ومجموعة الاختبارات الكاملة (235 اختباراً في 77 ملفاً) وبناء الإنتاج وفحص سلامة الفروق.
- لم تُفتح معاينة، ولم تُطبع أو تُنزّل مستندات، ولم تُعدّل سندات أو قرارات قائمة أثناء هذا التحقق؛ تبقى المعاينة وملف PDF بحاجة إلى تجربة قراءة آمنة من المستخدم على الإصدار المنشور.
