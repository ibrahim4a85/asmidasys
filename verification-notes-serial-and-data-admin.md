# تحقق واجهة الترقيم وإدارة البيانات

- تمت معاينة المسارات `/` و`/receipts` و`/settings` بعد التعديلات بتاريخ 2026-08-18.
- أعادت المسارات المحمية إلى صفحة تسجيل الدخول لعدم توفر جلسة مستخدم في بيئة المعاينة، ولذلك لم يُنفّذ تحقق مرئي داخل نموذج السند أو تبويب إدارة البيانات.
- تؤكد فحوصات الأنواع والاختبارات نجاح بناء المشروع، بينما تغطي اختبارات الوحدة تنسيق سيريالات القبض والصرف السنوية وصلاحية الحذف الانتقائي المفوّضة.
